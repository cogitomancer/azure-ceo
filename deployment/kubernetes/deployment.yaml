apiVersion: apps/v1
kind: Deployment
metadata:
  name: marketing-agent-api
  namespace: marketing-agents
spec:
  replicas: 3
  selector:
    matchLabels:
      app: marketing-agent-api
  template:
    metadata:
      labels:
        app: marketing-agent-api
    spec:
      serviceAccountName: marketing-agent-sa
      containers:
      - name: api
        image: acr.azurecr.io/marketing-agent-api:latest
        ports:
        - containerPort: 8000
        env:
        - name: AZURE_OPENAI_ENDPOINT
          valueFrom:
            secretKeyRef:
              name: azure-secrets
              key: openai-endpoint
        - name: COSMOS_DB_ENDPOINT
          valueFrom:
            secretKeyRef:
              name: azure-secrets
              key: cosmos-endpoint
        resources:
          requests:
            memory: "512Mi"
            cpu: "500m"
          limits:apiVersion: apps/v1
kind: Deployment
metadata:
  name: marketing-agent-api
  namespace: marketing-agents
spec:
  replicas: 3
  selector:
    matchLabels:
      app: marketing-agent-api
  template:
    metadata:
      labels:
        app: marketing-agent-api
    spec:
      serviceAccountName: marketing-agent-sa
      containers:
      - name: api
        image: acr.azurecr.io/marketing-agent-api:latest
        ports:
        - containerPort: 8000
        env:
        - name: AZURE_OPENAI_ENDPOINT
          valueFrom:
            secretKeyRef:
              name: azure-secrets
              key: openai-endpoint
        - name: COSMOS_DB_ENDPOINT
          valueFrom:
            secretKeyRef:
              name: azure-secrets
              key: cosmos-endpoint
        resources:
          requests:
            memory: "512Mi"
            cpu: "500m"
          limits:
            memory: "2Gi"
            cpu: "2000m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 10
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: marketing-agent-service
  namespace: marketing-agents
spec:
  selector:
    app: marketing-agent-api
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8000
  type: LoadBalancer
            memory: "2Gi"
            cpu: "2000m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 10
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: marketing-agent-service
  namespace: marketing-agents
spec:
  selector:
    app: marketing-agent-api
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8000
  type: LoadBalancer